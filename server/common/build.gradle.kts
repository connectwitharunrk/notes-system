/**
 * Cross-cutting primitives: error types, clock and id abstractions.
 * Deliberately Spring-free so both :domain and the adapters can depend on it.
 */
dependencies {
    implementation(libs.kotlin.reflect)
}
